# T{PP}-{NN} {Short title}

**Phase:** {PP} {Name}
**Priority:** P0 | P1 | P2
**Size:** S | M | L
**Depends on:** T{PP}-{NN} or `none`
**Blocks:** T{PP}-{NN} or `none`
**Launch:** Implement this ticket only. Do not start downstream tickets.

## Goal

One or two sentences. What is true when this ticket is done.

## Context

Why this exists. Point at `_shared/` contracts by path.

## Research

Cite IDs from `phases/_shared/references.md` (R01…). List URLs and **Search** fallbacks. Name the official term and the trainer delta. Required for scope, phraseology, alerts, and any user-facing copy.

## Scope

- Concrete work items.

## Out of scope

- Explicit non-goals so the implementing agent does not wander.

## Implementation notes

Constraints, suggested types, file layout, algorithms. Keep testable.

## Acceptance criteria

- [ ] **AC1 —** Given / when / then, or a binary observable.
- [ ] **AC2 —** …
- [ ] **AC3 —** Automated test exists for the happy path (unless marked Manual).
- [ ] **ACn — Research:** User-facing strings use glossary terms; a code comment cites analog + trainer delta.

## Test plan

- Unit:
- Integration:
- Manual:

## Suggested files

- `path/to/file.ts`


- Concrete work items.

## Out of scope

- Explicit non-goals so the implementing agent does not wander.

## Implementation notes

Constraints, suggested types, file layout, algorithms. Keep testable.

## Acceptance criteria

- [ ] **AC1 —** Given / when / then, or a binary observable.
- [ ] **AC2 —** …
- [ ] **AC3 —** Automated test exists for the happy path (unless marked Manual).

## Test plan

- Unit:
- Integration:
- Manual:

## Suggested files

- `path/to/file.ts`
