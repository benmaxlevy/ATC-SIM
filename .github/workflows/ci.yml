name: ci
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.6"
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install aircraft profile builder
        run: python -m pip install -r tools/aircraft-profiles/requirements.txt
      - run: npm ci
      - run: npm run ci
      - name: Check aircraft profile generator
        run: npm run aircraft:profiles:test && npm run aircraft:profiles:check
  speech-api:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: speech-api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements-ci.txt
      - run: pytest
        env:
          SPEECH_API_MOCK: "1"
