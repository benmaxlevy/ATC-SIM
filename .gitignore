node_modules
dist
coverage
*.local
.env
.env.*
!.env.example
*.log
.DS_Store
.tmp-stars-shots

# T03-13: Hub weight cache and Python debris (never commit models)
speech-api/.cache/
speech-api/.venv/
__pycache__/
.pytest_cache/
*.pyc

# Local FAA CIFP inputs, national derived source/index, pack intermediates
# (T04-32). Never imported by Vite. Do not commit national dumps.
.cifp/
FAACIFP18*
tools/cifp-import/out/

# Local CRC/vNAS cache copies and videomap converter intermediates (T04-36).
# Never commit ARTCCs/ZTL.json, VideoMaps GeoJSON, or generated dumps.
# Keep testdata/crc-videomaps/ fixtures.
.crc/
tools/crc-videomap-import/out/
