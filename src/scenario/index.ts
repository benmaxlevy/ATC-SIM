/**
 * Public API for `@scenario`.
 *
 * Legal now: `SCENARIO_PACKAGE` so barrel imports resolve.
 *
 * Later: KDEM JSON (T00-05), airport, spawn, maps.
 *
 * Import rule: `@scenario` may import `@core` only.
 */
export const SCENARIO_PACKAGE = "scenario";
